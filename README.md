# fast-api-dbs

## Install using pip

```
pip install -e .
```

## Install using poetry

```
poetry install
```

## How to run?
for pip install
```
uvicorn app.main:app --reload
```
for poetry
```
poetry run uvicorn app.main:app --reload
```
